#!/data/data/dev.termui/files/usr/bin/sh

if [ $# != 0 ]; then
	echo 'usage: termux-wake-unlock'
	echo 'Release the Termux wake lock to allow the CPU to sleep.'
	exit 1
fi

am startservice \
	--user 0 \
	-a dev.termui.service_wake_unlock \
	dev.termui/dev.termui.app.TermuxService \
	> /dev/null
